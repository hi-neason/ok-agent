spring:
  datasource:
    url: jdbc:h2:mem:ok_agent;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
ok-agent:
  security:
    api-key-encryption-key: test-only-encryption-key
